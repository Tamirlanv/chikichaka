services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-invision}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-invision}
      POSTGRES_DB: ${POSTGRES_DB:-invision}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-invision} -d ${POSTGRES_DB:-invision}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  # Node microservice: OCR (Tesseract + sharp + ffmpeg). Same image as production deploy path.
  certificate-validation:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.certificate-validation
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-invision}:${POSTGRES_PASSWORD:-invision}@postgres:5432/${POSTGRES_DB:-invision}
      CERT_VALIDATION_PORT: 4400
    volumes:
      - upload_data:/data/uploads
    ports:
      - "4400:4400"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4400/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 25s

  api:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.api
    env_file:
      - .env
    environment:
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key-for-invision-local-docker-only!!}
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-invision}:${POSTGRES_PASSWORD:-invision}@postgres:5432/${POSTGRES_DB:-invision}
      REDIS_URL: redis://redis:6379/0
      UPLOAD_ROOT: /data/uploads
      CERTIFICATE_VALIDATION_URL: http://certificate-validation:4400/certificate-validation/validate
    ports:
      - "8000:8000"
    volumes:
      - upload_data:/data/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      certificate-validation:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Admission job worker (Redis queue + heartbeat). Required for submit pipeline parity with production.
  worker:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.worker
    env_file:
      - .env
    environment:
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key-for-invision-local-docker-only!!}
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-invision}:${POSTGRES_PASSWORD:-invision}@postgres:5432/${POSTGRES_DB:-invision}
      REDIS_URL: redis://redis:6379/0
      UPLOAD_ROOT: /data/uploads
      CERTIFICATE_VALIDATION_URL: http://certificate-validation:4400/certificate-validation/validate
    volumes:
      - upload_data:/data/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      certificate-validation:
        condition: service_healthy
      api:
        condition: service_healthy
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.web
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
        API_INTERNAL_URL: http://api:8000
    environment:
      API_INTERNAL_URL: http://api:8000
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
    ports:
      - "3000:3000"
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres_data:
  upload_data:
